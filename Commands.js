// Define the array of slash commands that the bot will register with Discord
const commands = [
    {
        name: 'ping', // The name of the command (used as /ping)
        description: 'Check the bot ping' // Description that appears in Discord when typing the command
    }
];

export default commands; // Export the commands so they can be used for registration in other files
